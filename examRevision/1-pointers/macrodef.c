#ifndef hello_world
#define hello_world
#define myMacro 10
#define MacroTernary (a,b) ((a)>(b) ? (a):(b))
#define macroSum(a,b) ((a)+(a))
#endif

//* declarations does not require to assign elements value